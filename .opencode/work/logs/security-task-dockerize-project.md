# Security Scan Report

## Metadata
- **Issue:** task-dockerize-project
- **Timestamp:** 2026-05-17T18:07:00Z
- **Scanner:** Bandit (Python)
- **Files Scanned:** 11

---

## Summary

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 0 |
| Medium | 0 |
| Low | 0 |
| Info | 0 |

### Overall Status: PASS

---

## Automated Scan Results

**Bandit** (scripts/generate_agent_token.py): 0 issues identified.

All other files are configuration-only (Dockerfiles, docker-compose.yml, .dockerignore, .env.example, README.md) and contain no executable code.

---

## Manual Review

### Reviewed Files
| File | Type | Risk Level |
|------|------|------------|
| `.dockerignore` | Build config | None |
| `docker/backend/Dockerfile` | Docker config | None |
| `docker/frontend/Dockerfile` | Docker config | None |
| `docker/agent/Dockerfile` | Docker config | None |
| `scripts/entrypoint.sh` | Shell bootstrap | None |
| `scripts/generate_agent_token.py` | Python bootstrap | Low (see below) |
| `docker-compose.yml` | Compose config | None |
| `docker-compose.override.yml` | Compose config | None |
| `frontend/vite.config.ts` | Vite config | None |
| `.env.example` | Documentation | None |
| `README.md` | Documentation | None |

### A02: Cryptographic Failures
- [x] No hardcoded production secrets
- [x] Fernet key default is empty string (dev mode = plaintext) — documented as dev-only
- [x] JWT secret in docker-compose.yml is a documented dev value with "change in production" warning
- [x] Agent token prefix (`beacon_agent_`) is a non-secret identifier pattern

### A03: Injection
- [x] `generate_agent_token.py` makes HTTP calls to internal backend — no SQL, no shell injection
- [x] `entrypoint.sh` uses `exec` for uvicorn — no shell injection
- [x] Dockerfile COPY paths are static — no injection risk
- [x] `vite.config.ts` uses `import.meta.env` — no injection risk

### A07: Authentication Failures
- [x] `generate_agent_token.py` uses dev credentials (`admin@beacon.dev` / `admin123`) — these are well-documented seed data credentials (same as `scripts/seed.py`). This script runs ONLY inside the Docker dev environment.

### A10: SSRF
- [x] `generate_agent_token.py` connects to `BACKEND_URL` which defaults to `http://localhost:8000` (internal). In Docker it's `http://backend:8000` (internal bridge network). No user-controlled URLs.

---

## Passed Checks
- ✓ No SQL injection vulnerabilities (all DB access via SQLAlchemy ORM)
- ✓ No XSS vulnerabilities (no HTML rendering in new code)
- ✓ No hardcoded production secrets
- ✓ No command injection vectors
- ✓ No dependency changes (no new packages introduced)
- ✓ Input validation present (httpx validates URL formats)
- ✓ Agent token file written to Docker volume — not exposed externally

---

## Documented Caveats (Non-Blocking)
1. **Dev credentials in `generate_agent_token.py`**: `admin@beacon.dev` / `admin123` are seed-data dev credentials. This script only runs inside the dev Docker environment. Production deployments use different token provisioning.
2. **Dev JWT secret in `docker-compose.yml`**: The value `dev-secret-change-in-production-at-least-32-chars!!` is clearly marked as a dev default. Production must override via environment variable or secrets manager.
3. **FERNET_KEY empty in dev**: Connection config encryption uses plaintext when FERNET_KEY is empty. This is correct behavior for dev/test environments. Production must set a valid Fernet key.

---

## Gate Decision: PASS

All automated scans passed (0 Bandit issues). Manual review found no vulnerabilities. Documented caveats are design decisions for the dev environment, not security defects.

*Generated by security-checker skill*
