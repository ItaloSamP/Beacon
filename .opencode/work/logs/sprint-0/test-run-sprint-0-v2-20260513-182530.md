# Test Run: Sprint 0 — Beacon Foundations (v2)

**Run ID:** `20260513-182530`
**Date:** 2026-05-13T18:25:30-03:00
**Environment:** Windows 10, Python 3.13.4, Node.js, Docker 29.4.0
**PostgreSQL:** Docker (beacon-postgres) — connected ✅
**Redis:** Docker (beacon-redis) — running ✅

---

## Summary

| Type | Passed | Failed | Skipped | Total |
|------|--------|--------|---------|-------|
| Backend (Pytest) | 1* | 0* | 0 | 180 collected |
| Frontend (Vitest) | 148 | 3 | 0 | 151 |
| TypeScript (tsc) | PASS | 0 | 0 | — |
| Python Lint (Ruff) | — | 36 errors | 0 | — |
| Security (Bandit) | PASS | 0 | 0 | — |
| Rebranding Grep | PASS | 0 found | 0 | — |
| ESLint | N/A (not configured) | — | — | — |
| npm audit | — | 6 moderate | 0 | — |

> \* Backend tests hit a blocking infrastructure issue (email validation + asyncpg/Windows) and stopped after 3 errors (`--maxfail=3`). Only `test_health_returns_200` passed. The remaining 177 tests were not reached.

---

## ⛔ Gate G3: BLOCKED

**Backend tests cannot run to completion** due to a critical fixture failure. Frontend tests have 3 minor assertion failures. Gate G3 is NOT met.

---

## 1. Frontend Tests (Vitest)

### 1.1 Results
```
Test Files: 3 failed | 5 passed (8)
Tests:      3 failed | 148 passed (151)
Duration:   8.88s
```

### 1.2 Failed Tests

#### Failure 1: `AgentsListPage.test.tsx` — "should display agent version"
- **File:** `frontend/src/features/agents/__tests__/AgentsListPage.test.tsx:117`
- **Error:** `TestingLibraryElementError: Found multiple elements with the text: 0.1.0`
- **Root Cause:** Both mock agents ("Production Agent" and "Staging Agent") have version `"0.1.0"`, so `getByText('0.1.0')` matches multiple `<td>` elements.
- **Fix:** Use `expect(screen.getAllByText('0.1.0')).toHaveLength(2)` or assert on a specific row.

#### Failure 2: `AgentForm.test.tsx` — "should validate name is not just whitespace"
- **File:** `frontend/src/features/agents/__tests__/AgentForm.test.tsx:237`
- **Error:** `TestingLibraryElementError: Found multiple elements with the text: /name|nome|required|obrigatório|must|precisa/i`
- **Root Cause:** The broad regex matches multiple elements on the page (label "Name", validation messages, etc.).
- **Fix:** Use `getAllByText()` or make the regex more specific (e.g., target only validation error messages).

#### Failure 3: `DataSourcesListPage.test.tsx` — "should display agent column when data sources have agent_id"
- **File:** `frontend/src/features/datasources/__tests__/DataSourcesListPage.test.tsx:131`
- **Error:** `TestingLibraryElementError: Found multiple elements with the text: /agent/i`
- **Root Cause:** The regex `/agent/i` matches the column header `<th>Agent</th>` AND the data cell values "Production Agent" and "Staging Agent".
- **Fix:** Use `expect(screen.getAllByText(/agent/i).length).toBeGreaterThanOrEqual(3)` or target specific elements.

### 1.3 Passing Test Files (5)
- ✅ `useAuth.test.tsx` — 17 tests
- ✅ `Button.test.tsx` — 26 tests
- ✅ `Modal.test.tsx` — 22 tests
- ✅ `LoginPage.test.tsx` — (passed)
- ✅ `DataSourceForm.test.tsx` — (passed)

---

## 2. Backend Tests (Pytest)

### 2.1 Results
```
Collected: 180 tests
Executed: 1 passed, 3 errors (stopped at --maxfail=3)
Duration: 2.49s
```

### 2.2 Errors

#### Error 1: `TestCreateAgent.test_create_returns_201` — Email validation rejects `test@example.com`
- **File:** `tests/test_agents.py` (fixture: `tests/conftest.py:101`)
- **Error:** `AssertionError: Failed to register test user: {'data': None, 'error': 'invalid_email', 'message': 'Invalid email format'}`
- **Status:** 422 (expected 201)
- **Root Cause:** `app/application/auth_service.py:21` calls `validate_email(email)` from the `email_validator` library, which defaults to `check_deliverability=True`. The domain `example.com` is an IANA reserved domain that does not accept email, so the validator rejects it with `EmailUndeliverableError`.
- **Fix:** Change the test fixture email in `tests/conftest.py` to a deliverable domain, OR pass `check_deliverability=False` to `validate_email` in test configuration.

#### Error 2: `TestCreateAgent.test_create_minimal_fields` — Event loop closed
- **File:** `tests/test_agents.py` (fixture: `tests/conftest.py:49 — test_db`)
- **Error:** `AttributeError: 'NoneType' object has no attribute 'send'` / `RuntimeError: Event loop is closed`
- **Root Cause:** Cascading failure from Error 1. When the `auth_headers` fixture fails (Error 1), the event loop cleanup exposes asyncpg/Windows compatibility issues with Python 3.13's `ProactorEventLoop`.

#### Error 3: `TestCreateAgent.test_create_requires_auth` — InterfaceError
- **File:** `tests/test_agents.py` (fixture: `tests/conftest.py:49 — test_db`)
- **Error:** `sqlalchemy.exc.InterfaceError: cannot perform operation: another operation is in progress`
- **Root Cause:** Cascading failure from Error 1. SQLAlchemy + asyncpg concurrency issue on Windows after fixture failure.

### 2.3 Passing Test
- ✅ `tests/test_health.py::TestHealthEndpoint::test_health_returns_200`

---

## 3. TypeScript Type Check

**Command:** `npx tsc --noEmit` in `frontend/`
**Result:** ✅ ZERO errors — all TypeScript types are valid.

---

## 4. Python Lint (Ruff)

**Command:** `python -m ruff check .`
**Result:** 36 errors (35 fixable automatically)

All errors are minor:
- **F401** (unused imports): 26 instances — `alembic/env.py`, `alembic/versions/001_initial.py`, `alembic/versions/002_add_agents.py`, `app/application/datasource_service.py`, `app/presentation/api/routes/auth.py`, `tests/conftest.py`, `tests/test_auth.py`, `tests/test_auth_service.py`, `tests/test_migrations.py`
- **F541** (f-string without placeholders): 4 instances — `scripts/seed.py`
- **E402** (import not at top): 1 instance — `tests/test_auth.py`

> Fix with: `python -m ruff check --fix .`

---

## 5. Security Scan (Bandit)

**Command:** `python -m bandit -r app/`
**Result:** ✅ ZERO issues identified (1377 lines scanned)

| Severity | Count |
|----------|-------|
| High | 0 |
| Medium | 0 |
| Low | 0 |

---

## 6. Rebranding Grep

**Search terms:** "Data Health Monitor", "dhm_", "DHM" (standalone)
**Scope:** `app/`, `frontend/src/`, `agent/`, `tests/`, `scripts/` (`.py`, `.ts`, `.tsx`, `.js`, `.jsx`, `.toml`, `.yml`, `.yaml`, `.html`, `.css`, `.json`, `.ini`, `.env`)
**Result:** ✅ ZERO matches in all source directories.

---

## 7. ESLint

**Result:** N/A — ESLint is not configured. No `eslint.config.js` or `.eslintrc.*` file found. The `package.json` has a `lint` script calling `eslint . --ext ts,tsx` but the config is missing.

---

## 8. npm audit

**Result:** 6 moderate severity vulnerabilities

All related to `esbuild <= 0.24.2` (transitive via vitest/vite-node):
- GHSA-67mh-4wv8-2f99: "esbuild enables any website to send any requests to the development server"
- Fix requires upgrading vitest to v4.x (breaking change)

---

## 9. Docker Compose

**Command:** `docker compose up -d`
**Result:** ✅ Both services running
- `beacon-postgres-1`: PostgreSQL 16, healthy
- `beacon-redis-1`: Redis 7, healthy

---

## 10. Backend Coverage

**Result:** UNAVAILABLE — pytest exited early (3 errors). Coverage cannot be generated until all fixtures pass.

---

## 11. Frontend Coverage

**Command:** `npx vitest run --coverage`
**Result:** Coverage report generated but requires `@vitest/coverage-v8@2` (matching vitest v2.1.9). The coverage threshold check is deferred until after tests pass.

---

## Root Cause Analysis

### Blocking: Backend
The `auth_headers` fixture in `tests/conftest.py:101` uses `email = "test@example.com"` which is rejected by `email_validator.validate_email()` with deliverability check enabled. This prevents ALL authenticated tests from running.

**Two fixes needed:**
1. **Primary:** Change `"test@example.com"` to a deliverable test email (e.g., `"test@beacon-test.local"` or use `check_deliverability=False`)
2. **Secondary:** Consider using `scope="session"` for the `test_db` fixture to avoid the asyncpg/Windows event loop issues on Python 3.13.

### Non-blocking: Frontend
3 assertion errors due to ambiguous `getByText()` selectors. All are mechanical fixes — change to `getAllByText()` with length assertions or more specific selectors.

---

## Recommended Actions

1. **[CRITICAL]** Fix `tests/conftest.py:96` — change test email from `"test@example.com"` to a deliverable domain or add `check_deliverability=False`
2. **[HIGH]** Fix 3 frontend test assertions (use `getAllByText` or more specific selectors)
3. **[MEDIUM]** Run `ruff check --fix .` to auto-fix 35/36 lint errors
4. **[LOW]** Configure ESLint or remove the `lint` script from `frontend/package.json`
5. **[LOW]** Run `npm audit fix --force` to address esbuild vulnerability (or accept as dev-only risk)
